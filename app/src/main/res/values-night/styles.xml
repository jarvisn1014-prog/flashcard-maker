<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Flashcards" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Dark theme: status bar icons should be light, so windowLightStatusBar=false -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>